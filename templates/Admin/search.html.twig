{% extends "admin/base.html.twig" %}

{% block main %}

	<h1>Le résultat de la recherche
		{{ term }}
		est :</h1>

	{% for band in bands %}
		<h2>{{ brand.name }}
			{{ brand.firstname }}</h2>
	{% endfor %}

{% endblock %}