{% extends "front/base.html.twig" %}

{% block main %}

	{% for groupe in groupes %}
		<h2>
			<a href="{{ path("category_show", {'id' : groupe.id } ) }}">{{groupe.name}}</a>
		</h2>

	{% endfor %}

{% endblock %}